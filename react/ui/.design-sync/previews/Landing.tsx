// Landing — the backend-less public front door ("Inference you own.").
// Self-fetches readiness/version; DemoProvider answers from showroom fixtures.
import { Landing } from '@hugpy/ui'

export function Default() {
  return <Landing />
}
