ApiAccess from @hugpy/ui. Use via `window.HugpyUI.ApiAccess` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<DemoProvider>` (full provider chain in README.md — components read theme/i18n from that context).

## Props

```ts
interface ApiAccessProps {
  models?: any[];
  embedded?: boolean;
}
```
