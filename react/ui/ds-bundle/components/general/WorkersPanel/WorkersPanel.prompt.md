WorkersPanel from @hugpy/ui. Use via `window.HugpyUI.WorkersPanel` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<DemoProvider>` (full provider chain in README.md — components read theme/i18n from that context).

## Props

```ts
interface WorkersPanelProps {
  models?: any[];
  embedded?: boolean;
}
```
