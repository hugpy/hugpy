import * as React from 'react';

/**
 * LoginForm — from @hugpy/ui@0.2.0.
 */
export interface LoginFormProps {
  [key: string]: unknown;
}

export declare const LoginForm: React.ComponentType<LoginFormProps>;
