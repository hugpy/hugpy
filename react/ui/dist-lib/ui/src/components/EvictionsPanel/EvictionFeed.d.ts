/**
 * @param selectedModel model_key currently chosen in the list above; highlighted
 *                      (never filtered away) wherever it appears in a pass.
 */
export default function EvictionFeed({ selectedModel }: {
    selectedModel?: any;
}): import("react").JSX.Element;
